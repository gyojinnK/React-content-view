import "./Header.css";
import Navbar from "./Navbar";

const Header: React.FC = () => {
    return (
        <div className="headerWrap">
            <Navbar />
        </div>
    );
};

export default Header;
